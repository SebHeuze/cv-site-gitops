apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../base
# Production uses base values - no patches needed
# Base already configured for production (3 instances, 20Gi storage, etc.)
