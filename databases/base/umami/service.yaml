apiVersion: v1
kind: Service
metadata:
  name: umami-db
  namespace: monitoring-prd
  labels:
    app: umami-db
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: postgres
  selector:
    app: umami-db
