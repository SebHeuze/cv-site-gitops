apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: trading-db
  namespace: cv-site-prd
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: trading-db
  podMetricsEndpoints:
    - port: metrics
