apiVersion: v1
kind: Service
metadata:
  name: binance-ingester
  namespace: cv-site-prd
  labels:
    app: binance-ingester
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: binance-ingester
