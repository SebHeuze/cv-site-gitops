apiVersion: v1
kind: Service
metadata:
  name: akhq
  namespace: monitoring-prd
  labels:
    app: akhq
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: akhq
