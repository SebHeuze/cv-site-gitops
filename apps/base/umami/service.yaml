apiVersion: v1
kind: Service
metadata:
  name: umami
  namespace: monitoring-prd
  labels:
    app: umami
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: umami
