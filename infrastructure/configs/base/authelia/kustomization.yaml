apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
  - secrets.yaml
  - pvc.yaml
configMapGenerator:
  - name: authelia-config
    namespace: authelia
    files:
      - configuration.yml
