apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - sources
  - cert-manager
  - traefik
  - strimzi-kafka
  - cloudnativepg
  - external-secrets
  # Vault is deployed externally on Proxmox VM - see INSTALL_VAULT.md
