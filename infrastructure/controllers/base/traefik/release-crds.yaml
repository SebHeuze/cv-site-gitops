apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: traefik-crds
  namespace: traefik
spec:
  interval: 30m
  chart:
    spec:
      chart: traefik-crds
      # Pinned to 1.15.x: v1.16.0 chart archive exceeds the 1MiB Helm release Secret limit
      version: "1.15.x"
      sourceRef:
        kind: HelmRepository
        name: traefik
        namespace: flux-system
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
